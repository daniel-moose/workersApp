<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aplicatie">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activitateAcceptareComanda"></activity>
        <activity android:name=".activitateComandaCurenta" />
        <activity android:name=".activitateComanda" />
        <activity android:name=".activitateRezervare" />
        <activity android:name=".activitateServicii" />
        <activity android:name=".AdaugareServicii" />
        <activity android:name=".activitateConversatie" />
        <activity android:name=".paginaPrincipalaLucratori" />
        <activity android:name=".resetareParola" />
        <activity android:name=".paginaPrincipala" />
        <activity android:name=".paginaLogin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>